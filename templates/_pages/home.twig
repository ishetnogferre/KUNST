{# ========================================================================== #}
{# Props #}
{# ========================================================================== #}
{% set title = entry.title|default("Hallo, dit is de home!") %}

{# ========================================================================== #}
{# Query #}
{# ========================================================================== #}
{# TODO: Prepare query with parameters #}
{% set itemsQuery = craft.entries({

}) %}

{# TODO: Add filtering #}
{% set filters = [] %}

{# Gather the items from our query #}
{% set items = itemsQuery.collect() %}

{# ========================================================================== #}
{# Template #}
{# ========================================================================== #}
{% extends "_layout.twig" %}

{% block header %}
	<div class="bg-black text-center py-12 px-20">
		<h1 class="font-black text-5xl text-white">
			{{- title -}}
		</h1>
	</div>
{% endblock %}

{% block content %}
	<section class="bg-black pb-14">
		{# TODO: add highlighted item #}
	</section>

	<section class="bg-white">
		{# TODO: add filtering for paintings #}

		{# TODO: add overview of paintings #}

		{# TODO: add pagination #}
	</section>
{% endblock %}